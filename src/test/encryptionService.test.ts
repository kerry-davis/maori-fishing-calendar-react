// Legacy passphrase-based encryption tests removed (Option B).
// Deterministic encryption is covered in encryptionDeterministic.test.ts.
// This placeholder intentionally contains no runnable tests.
import { describe, it } from 'vitest';

describe.skip('legacy encryptionService (removed)', () => {
  it('placeholder', () => {});
});
